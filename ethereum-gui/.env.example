# =============================================================================
# ethereum-gui — Vite Environment Variables Template
# =============================================================================
# This file is safe to commit. It contains no real credentials.
# Copy it to ethereum-gui/.env and fill in your values:
#   cp .env.example .env
#
# Only VITE_-prefixed variables are exposed to the browser bundle.
# =============================================================================

VITE_ETH_RPC_ENDPOINT=http://localhost:8001


